# ClipPing 用户手册

## 目录

1. [软件简介](#软件简介)
2. [功能特点](#功能特点)
3. [系统要求](#系统要求)
4. [安装与卸载](#安装与卸载)
5. [基本操作](#基本操作)
6. [设置说明](#设置说明)
7. [覆盖层类型](#覆盖层类型)
8. [常见问题](#常见问题)
9. [技术支持](#技术支持)

---

## 软件简介

ClipPing 是一款运行在 Windows 系统上的剪贴板视觉通知工具。每当您复制（Ctrl+C）或剪切（Ctrl+X）内容时，ClipPing 会在当前活动窗口上显示一个短暂的视觉覆盖层动画，给您即时的反馈，确认复制操作已成功执行。

---

## 功能特点

- **视觉反馈**：复制内容后，当前窗口边缘会出现彩色覆盖层闪烁
- **多种样式**：提供 6 种不同的覆盖层动画效果
- **自定义颜色**：可自由选择覆盖层颜色
- **开机自启**：支持设置开机自动运行
- **轻量小巧**：占用系统资源极少，后台静默运行
- **系统托盘**：运行在系统托盘区，不打扰正常工作

---

## 系统要求

| 项目 | 要求 |
|------|------|
| 操作系统 | Windows 10 版本 1607 或更高版本 |
| 系统架构 | x64（64位） |

---

## 安装与卸载

### 安装方式

#### 方式一：使用 Winget（推荐）

在 Windows 终端或 PowerShell 中执行：

```
winget install KevinGosse.ClipPing
```

#### 方式二：安装包

1. 访问 GitHub releases 页面
2. 下载 `ClipPing-Setup.exe`
3. 双击运行安装程序
4. 按照提示完成安装

#### 方式三：便携版

1. 下载 `ClipPing.exe`
2. 双击直接运行，无需安装

### 卸载方式

- **开始菜单卸载**：开始 → 设置 → 应用 → ClipPing → 卸载
- **设置中退出**：右键托盘图标 → 退出 ClipPing
- **任务管理器结束**：Ctrl+Shift+Esc 打开任务管理器，结束 ClipPing 进程

---

## 基本操作

### 启动与运行

ClipPing 启动后会自动最小化到系统托盘区（时钟旁边）。您可以在托盘区看到 ClipPing 的图标。

### 托盘图标操作

| 操作 | 功能 |
|------|------|
| 左键双击 | 打开设置窗口 |
| 右键单击 | 弹出快捷菜单 |

### 快捷菜单

右键点击托盘图标，可看到以下选项：

- **设置...** - 打开设置对话框
- **关于...** - 查看版本信息和作者
- **退出** - 关闭 ClipPing

---

## 设置说明

点击托盘图标选择"设置..."进入设置对话框。

### 外观设置

#### 覆盖层颜色

点击"选择..."按钮，打开 Windows 颜色选择器，选择您喜欢的覆盖层颜色。

#### 覆盖层类型

选择覆盖层显示的效果样式，详见下一节。

#### 预览

点击"预览"按钮，可以立即在当前窗口上预览覆盖层效果。

### 常规设置

#### 开机自启动

勾选"开机自启动"，ClipPing 将在 Windows 启动时自动运行。

### 汉化信息

设置窗口底部显示：
- **汉化：科控物联  QQ:2492123056**
- **原作者：Kevin Gosse**

---

## 覆盖层类型

ClipPing 提供 6 种覆盖层效果：

| 类型 | 说明 | 效果预览 |
|------|------|----------|
| 顶部 | 窗口顶部显示渐变条 | 从上往下渐变消失 |
| 底部 | 窗口底部显示渐变条 | 从下往上渐变消失 |
| 左侧 | 窗口左侧显示渐变条 | 从左往右渐变消失 |
| 右侧 | 窗口右侧显示渐变条 | 从右往左渐变消失 |
| 边框 | 窗口四周显示实线边框 | 8像素宽边框闪烁 |
| 光晕 | 窗口四周显示发光效果 | 四边渐变光晕环绕 |

覆盖层显示时间约 400 毫秒（淡入 100ms + 淡出 300ms），不会影响您的正常操作。

---

## 常见问题

### Q: 为什么复制内容后没有看到覆盖层？

1. 检查 ClipPing 是否正在运行（托盘区是否有图标）
2. 如果当前没有活动窗口，覆盖层不会显示
3. 某些全屏应用可能无法显示覆盖层

### Q: 如何更改覆盖层颜色？

打开设置 → 点击"选择..."按钮 → 在颜色选择器中选择颜色 → 点击"确定"

### Q: 如何取消开机自启动？

打开设置 → 取消勾选"开机自启动" → 点击"确定"

### Q: 覆盖层挡住了窗口内容怎么办？

1. 减小覆盖层透明度（选择更浅的颜色）
2. 改用"光晕"类型，覆盖层会在显示后快速淡出

### Q: 不想用了如何完全关闭？

右键托盘图标 → 选择"退出" → ClipPing 将从内存中卸载

---

## 技术支持

- **汉化维护**：科控物联
- **联系 QQ**：2492123056
- **GitHub 仓库**：[jingshui127/ClipPing](https://github.com/jingshui127/ClipPing)
- **原作者**：Kevin Gosse

---

## 版本信息

- **当前版本**：2.3.0
- **许可证**：MIT
- **构建日期**：2026年5月
